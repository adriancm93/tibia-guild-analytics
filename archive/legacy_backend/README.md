# Legacy Backend

This folder contains the earlier FastAPI backend used during local development and earlier architecture phases.

It is no longer part of the production deployment.

Current production frontend reads directly from Supabase REST API views, so no always-running backend server is required.

This code is retained for historical reference only.
